# Analysis

Scripts Python (pandas), processo separado do backend, lendo/escrevendo no mesmo Postgres. Responsável por: detecção de vazamento (MNF com baseline adaptativo por unidade, Entrega 3), previsão de consumo e geração de recomendações (Entrega 4).
